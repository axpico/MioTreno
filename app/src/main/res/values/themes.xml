<resources>
    <!-- Solo scuro: la finestra è già nera prima che Compose disegni, niente flash bianco. -->
    <style name="Theme.MioTreno.Base" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0A0A0F</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.MioTreno" parent="Theme.MioTreno.Base" />
</resources>
